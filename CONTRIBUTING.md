# CatCommStudio 协作与提交规范

本项目使用 [Conventional Commits 1.0.0](https://www.conventionalcommits.org/zh-hans/v1.0.0/) 描述变更。本文中的必填范围、中文正文和验证记录属于项目补充约定。分支与版本管理见 [Git 开发与发布流程](docs/git-workflow.md)。

## 1. 提交格式

```text
<type>(<scope>)[!]: <简短说明>

背景：
说明需要解决的问题，或本次变更的目的。

变更：
- 描述最终行为或具体修改。
- 说明有必要保留的设计取舍。

验证：
- 记录实际执行的检查、测试及结果。
- 未执行的相关验证需说明原因。

Refs: #123
```

- `type`、`scope` 使用小写英文；说明与正文默认使用中文。
- 标题必须带范围，建议不超过 72 个字符，不以句号结尾。
- 标题说明具体动作及对象，避免仅写“更新”“修改”“优化”或“修复问题”。
- 正文必须交代背景、变更和验证，可以使用上面的三个段落；标题与正文之间留空行。
- `Refs`、`Closes` 等脚注按需添加，禁止编造 Issue 编号或验证结果。
- 破坏兼容性的变更在标题加 `!`，并增加 `BREAKING CHANGE:` 脚注，说明影响和迁移方式。
- Git 自动生成的合并、回退消息在正式提交前也应按本规范整理。个人任务分支上的临时 `fixup!` 提交需在合入共享分支前整理。

## 2. 提交类型

| 类型 | 用途 |
| --- | --- |
| `feat` | 新增用户可用的功能 |
| `fix` | 修复已有行为的错误 |
| `docs` | 需求、设计、使用说明等文档变更 |
| `style` | 不改变行为的代码格式调整；UI 外观功能使用 `feat(ui)` 或 `fix(ui)` |
| `refactor` | 保持功能行为的代码结构调整 |
| `perf` | 有依据的性能改进 |
| `test` | 增加或调整测试 |
| `build` | 构建系统、编译配置与依赖调整 |
| `ci` | 持续集成和自动化工作流调整 |
| `chore` | 仓库维护、许可证、协作规范、发布准备等 |
| `revert` | 回退变更；正文记录原提交及回退原因 |

范围建议使用实际模块名，例如 `planning`、`license`、`workflow`、`core`、`serial`、`ble`、`tcp`、`udp`、`usb`、`hid`、`websocket`、`mqtt`、`ui`、`session`、`log`、`protocol`、`release`、`merge`。可以按实际职责补充，保持同一模块命名一致。

## 3. 示例

以下为格式示例，不表示相应功能已经实现。

```text
feat(session): 支持保存和恢复串口连接配置

背景：
重复调试同一设备时需要反复填写串口参数。

变更：
- 保存端口、波特率及数据位等连接参数。
- 恢复会话时填充参数，由用户触发连接。

验证：
- 配置保存与恢复测试通过。
- 串口设备实测未执行，本次环境未连接测试设备。
```

```text
docs(planning): 补充 BLE 服务与特征操作流程

背景：
BLE 的读写与订阅需要明确目标特征及其支持的操作。

变更：
- 补充设备扫描、服务选择、特征读写和订阅流程。
- 标注待确认的错误恢复行为。

验证：
- 文档本地链接和空白检查通过。
- 仅修改文档，无需运行应用测试。
```

## 4. 提交边界与验证

每个提交应表达一个可以独立理解和回退的改动。不同职责的改动应拆分；功能实现与保证该功能正确性的相关测试可以放在同一提交。

提交前查看 `git status --short` 和 `git diff`，按具体文件暂存，再执行：

```bash
git diff --cached --stat
git diff --cached --check
```

检查暂存内容，避免包含无关文件、凭据、构建产物和临时分析文件。根据改动运行相应检查：文档检查链接与格式，代码运行相关构建和测试，通信行为按实际设备条件验证。提交说明记录结果和适用边界。

## 5. 提交模板

仓库提供 [.gitmessage](.gitmessage)。在仓库根目录执行以下命令，为当前克隆启用模板：

```bash
git config --local commit.template "$PWD/.gitmessage"
```

此命令适用于 Bash / Git Bash。其他终端可将 `$PWD/.gitmessage` 替换为该文件的绝对路径。

运行 `git commit` 后填写标题、背景、变更与验证，删除不适用的占位提示。使用命令行提交多段说明时，先将完整说明写入 UTF-8 文本文件，再运行 `git commit -F <说明文件>`。

模板用于提示填写内容；当前未配置自动提交校验钩子或 CI 校验。使用 `-m`、`-F` 或图形工具提交时仍需遵守相同规范。
