# 项目协作约定

## 项目背景

CatCommStudio 是基于 SimpleCommKit 通信核心库规划的多通信调试工具，目前处于需求和交互设计阶段。产品资料见 `docs/product-definition.md` 与 `docs/communication-capabilities.md`。区分已确认需求、设计建议和待验证能力。

## Git 提交

- 创建提交前阅读并遵守 `CONTRIBUTING.md`。
- 使用 `type(scope)[!]: 简短说明` 标题，类型和范围使用小写英文，正文默认中文。
- 正文交代背景、具体变更及实际验证结果，不能仅提供笼统标题或编造检查结果。
- 按可独立理解和回退的职责划分提交，按具体文件暂存，并检查暂存差异。
- 多段提交说明先写入 UTF-8 文件，再使用 `git commit -F` 提交。

## 分支与发布

- 遵守 `docs/git-workflow.md`：`main` 用于稳定发布，`develop` 用于日常集成。
- 日常任务从 `develop` 创建对应的任务分支；本次协作规范初始化是文档中约定的一次例外。
- `release/<版本号>` 在确定发布范围后创建；紧急修复从当前发布对应的 `main` 提交创建 `hotfix/*`。
- 不改写已共享分支或已发布标签的历史。
- 按用户当次授权执行提交、推送和发布；已明确授权的操作无需重复确认。
